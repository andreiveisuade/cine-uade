package ar.uade.cine.model.funciones;

/**
 * Formato de imagen de la función. Distinto de TipoSala: la sala dice qué puede
 * proyectar, la función dice qué se está proyectando. Una sala 3D también da 2D.
 */
public enum Proyeccion {
    DOS_D,
    TRES_D
}
